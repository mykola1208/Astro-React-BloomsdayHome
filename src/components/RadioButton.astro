---
const { label, id } = Astro.props;
---

<div
  class="flex items-center p-3 border border-gray-200 dark:border-gray-700 bg-white radio-button"
>
  <input
    id={id}
    type="radio"
    value={label}
    name="bordered-radio"
    class="w-4 h-4 text-blue-600 bg-gray-100 border-gray-300 focus:ring-blue-500 dark:focus:ring-blue-600 dark:ring-offset-gray-800 focus:ring-2 dark:bg-gray-700 dark:border-gray-600"
  />
  <label for={id} class="text-sm font-normal text-darkgreen px-4">{label}</label
  >
</div>
<style>
  .radio-button {
    border-radius: 50px;
  }
</style>
